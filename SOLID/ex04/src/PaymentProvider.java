public interface PaymentProvider {
    String pay(double amount);



    /*
    *
    * Use an interface to define a contract for what a class can do.

Use an abstract class to define a base class for what a class is, providing shared behavior and state.
*
*
    * */
}
